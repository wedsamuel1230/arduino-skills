# Arduino skills

Use the canonical skills under `skills/`. For physical or multi-session work,
start with `skills/embedded-project-loop/SKILL.md`, then use
`skills/arduino-workflow-router/SKILL.md` for combined or underspecified work.
Use a focused specialist when the board and proof stage are already known.
Keep provider metadata in `.claude-plugin/` and do not copy skill bodies.
