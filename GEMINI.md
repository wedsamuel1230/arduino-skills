# Arduino skills

The shared source of truth is `skills/`. Load only the specialist skill and
references needed for the request. Preserve the board intake, ordered
`constexpr int` declarations, C/C++ embedded-first orientation, and explicit
hardware evidence gates.

For physical or multi-session work, load `skills/embedded-project-loop/SKILL.md`
first so the durable goal, next todo, and user-owned evidence gate exist before
specialist routing.
